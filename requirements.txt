# Python 3.10.*

networkx == 2.*
pandas >= 1.3.4
praw >= 7.4.0
scikit-learn
tensorflow == 2.10.*
tensorflow_addons
matplotlib
tensorboard
argparse
scipy
numpy
tweepy
psaw
PySimpleGUI

# snscrape, but it needs to be installed like this:
# pip install git+https://github.com/JustAnotherArchivist/snscrape.git
# because this is the dev version